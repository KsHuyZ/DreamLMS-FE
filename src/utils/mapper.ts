import { ELevel } from '@/types';

export const levelCourseMap = new Map<string, string>([
  [ELevel.ALL, 'All level'],
  [ELevel.BEGINNER, 'Beginner'],
  [ELevel.EXPERT, 'Expert'],
  [ELevel.MIDDLE, 'Middle'],
]);
